name: frontend ci

on:
  push:
    paths:
      - 'pwa.html'
      - 'runtime-config.js'
      - 'food-catalog.js'
      - 'pwa-enhancements.js'
      - 'pwa-api.js'
      - 'pwa-sync.js'
      - 'pwa-state-sync.js'
      - 'pwa-profile-sync.js'
      - 'pwa-account-sync.js'
      - 'ui-polish.js'
      - 'service-worker.js'
      - '.github/workflows/frontend-ci.yml'
  pull_request:
    paths:
      - 'pwa.html'
      - 'runtime-config.js'
      - 'food-catalog.js'
      - 'pwa-enhancements.js'
      - 'pwa-api.js'
      - 'pwa-sync.js'
      - 'pwa-state-sync.js'
      - 'pwa-profile-sync.js'
      - 'pwa-account-sync.js'
      - 'ui-polish.js'
      - 'service-worker.js'
      - '.github/workflows/frontend-ci.yml'

jobs:
  syntax:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Check JavaScript syntax
        run: |
          node --check runtime-config.js
          node --check food-catalog.js
          node --check pwa-enhancements.js
          node --check pwa-api.js
          node --check pwa-sync.js
          node --check pwa-state-sync.js
          node --check pwa-profile-sync.js
          node --check pwa-account-sync.js
          node --check ui-polish.js
          node --check service-worker.js
      - name: Check PWA asset wiring
        run: |
          grep -q 'runtime-config.js?rev=17' pwa.html
          grep -q 'pwa-api.js?rev=17' pwa.html
          grep -q 'pwa-sync.js?rev=17' pwa.html
          grep -q 'pwa-state-sync.js?rev=17' pwa.html
          grep -q 'pwa-profile-sync.js?rev=17' pwa.html
          grep -q 'pwa-account-sync.js?rev=17' pwa.html
          grep -q "healthy-action-pwa-v17" service-worker.js
